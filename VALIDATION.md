# DEV 0.1 验收记录

日期：2026-09-05。环境：Windows x64，天工 CAD 2025 高级版 225.2.1.181。

**状态：代码、原生插件注册和自动化建模已实现；尚未达到完整可视化验收，不能称为最终发布版。**

## 已通过

最终自动化日志：工作目录的 `artifacts/native-test-20260905-163002.log`（交付包内为 `examples/native-test-20260905-163002.log`），42 条 PASS，退出码 0。包含 384 种选四面顺序/法向组合的内部遍历；并非 42 次人工操作。

| 检查 | 证据 / 结果 |
|---|---|
| 编译 | .NET Framework x64 编译，无错误、无警告 |
| 原生插件加载 | 在独立 CAD 实例中加载；原生命令注册为 62014，首次创建按钮无接口错误 |
| 卸载 | 当前用户的插件 CLSID 注册键移除成功；不会删除生成的板子文件 |
| 基本尺寸 | 500 × 300 mm 框口、间隙 1 mm、总厚度 6 mm，实体为 498 × 298 × 6 mm |
| 对称位置 | CAD 实体范围实际核查为中面两侧各 3 mm |
| 可编辑性 | 重开 PAR 后修改拉伸厚度为 8 mm、草图宽度为 520 mm，实际实体尺寸随之更新 |
| 文件重开 | 保存并重开普通装配，板子实例数量和变换矩阵保持正确 |
| 几何合法性 | 重复面、非矩形面组、零/NaN 厚度、负间隙、间隙导致长宽非正数均拒绝 |
| 选面顺序 | 全部 24 种顺序 × 16 种法向组合结果一致 |
| 装配引用 | 四个实际型材实例的平面读取正确；同一 PAR 的不同实例不会混淆 |
| 嵌套装配 | 两份框架实例之一做空间旋转和平移，通过子实例拓扑引用读取坐标，生成板子准确放入对应框架 |
| 定位点 | 实际顶点、直线边中点、装配基准点、圆形边圆心读取通过 |
| 曲面拒绝 | 用圆柱曲面实际验证平面过滤逻辑 |
| 参数窗口 | 通过真实 CAD 引用调用选择事件处理流程；三维预览实际绘制并导出窗口图检查 |
| 取消 | 关闭选择窗口后，实例数不变，高亮对象归零；本次测试前后的 Dirty 均为 True，没有强行清除用户的未保存标记 |
| 重名与失败清理 | 已有文件拒绝覆盖；不存在的目录不创建实例；模拟保存期间并发创建同名文件时，保留对方文件且清理暂存文件，无新装配引用 |
| 源文件保护 | 所有建模测试使用新建测试文件；两种测试型材 PAR 的运行前后 SHA-256 一致 |

最后测试夹具目录为 `artifacts/cad-20260905-162943`。其中 `Panel.par` 是修改尺寸后的 520 × 298 × 8 mm 样件；`NestedPanel.par` 是嵌套装配生成的 498 × 298 × 6 mm 板子（局部长宽方向可能互换）。

插件 DLL SHA-256：

```text
CE649F128408F98D9E24AAA4BF073770544AAF9E76A7E988B9A6190540A7246B
```

## 未完成的验收

- 实际用鼠标点击原生按钮、逐面选取、捕捉点，再经保存对话框完成生成的全过程可视化复核。自动化测试调用了真实 CAD 接口和选择事件处理方法，但没有用鼠标完成整条交互链。
- 圆弧中心与零件内部基准点的单独现场操作；目前圆形边圆心和装配基准点已实测，圆弧采用相同解析圆几何逻辑。
- 在用户实际围栏总装副本上检查操作习惯、遮挡选面、大装配性能和重新打开嵌套装配后的显示。
- 非默认 DPI、多个显示器，以及其他天工 CAD 版本的兼容性。

窗口截图工具多次返回 `SetIsBorderRequired failed: 不支持此接口 (0x80004002)`，因此没有用盲点坐标进行生产窗口操作。已检查 CAD 自身导出的模型图和本插件导出的窗口图，这不能替代上述全过程可视化验收。

## 实现调整

本机视口的 OpenGL/GDI 绘图回调不触发，最终采用参数窗口内可旋转、缩放的三维预览，显示实际选面边界、板子和定位点。它不向装配写入预览特征。

框架变动后的自动关联、自动替换旧板、干涉检查、斜边板、倒角和孔不属于首版。
